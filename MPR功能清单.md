# DICOM MPR 工具功能清单（Java + VTK）

> 标记：★ = MVP 必需；☆ = 进阶。
> 说明：本文档为可交互清单。后续每当我解释某个条目时，会把解释文本追加到该条目下方的「说明」小节，逐步形成完整设计说明。

---

## A. DICOM 数据层

### A1 ★ 读取 DICOM 文件/目录/多帧序列，解析全部标签

### A2 ★ 按 SeriesInstanceUID 分组，按 ImagePositionPatient 沿法向量排序切片

**说明**：这是把一堆 DICOM 文件还原成一个有序三维体数据的关键两步。一次扫描输出几十到几百张二维切片，程序必须先知道"哪些是同一组、谁在前谁在后"。

1. **按 SeriesInstanceUID 分组**：一个目录/压缩包常混有多次检查的图。每张 DICOM 有 `SeriesInstanceUID (0020,000E)`，是这一序列的全局唯一 ID。同一 UID 的文件 = 同一次扫描、参数一致的一叠切片。按它分桶，才能对每一叠各自重建体数据，避免不同序列混在一起。

2. **按 ImagePositionPatient 沿法向量排序**：`ImagePositionPatient (0020,0032)` 记录每张切片左上角体素在患者坐标系（LPS）中的 (x,y,z)，单位 mm。层叠方向（法向量）由 `ImageOrientationPatient (0020,0037)` 决定：

   ```
   行方向 row = (x1,y1,z1)
   列方向 col = (x2,y2,z2)
   法向量 normal = row × col   （叉积，垂直于切片平面，即层叠方向）
   ```

   排序方法：把每张切片位置投影到法向量上（点积）得到标量距离 `d = position · normal`，按 `d` 从小到大排序，即从脚到头的物理顺序。

**为什么不用文件名或 InstanceNumber 排**：文件名任意、可能乱序；`InstanceNumber (0020,0013)` 有些设备缺失或不准；按空间坐标几何排序最可靠，还能顺带算出实际层间距、发现重复层或缺失层/层间距不均。

一句话：**分组决定"哪些图是一套"，法向量投影排序决定"这套图从下到上的物理顺序"**，二者是构建正确三维体数据、进而做 MPR 的前提。

**通俗版补充**：

把人体想象成一个大房间，房间里有固定的三维坐标（LPS，单位 mm）。人体里每个点都有一个"门牌号" (x, y, z)——这就是**患者坐标系**，它跟扫描仪怎么摆、切片怎么切都无关，是一个固定的空间。

现在想象你手里有一叠照片（就是那些切片）：

- **ImagePositionPatient = 每张照片左上角贴在房间里的哪个坐标**。
  例如某张切片说"我左上角在 (100, 50, -25) mm"，意思就是这个角在房间的那个位置。有了左上角 + 每像素间距 (PixelSpacing) 和朝向，整张照片其余像素的位置就都能推出来。

- **ImageOrientationPatient = 这张照片在房间里是怎么转的、朝哪边**。
  它给 6 个数，其实是两个方向：
  - 前 3 个数：照片里"向右"这个方向，指向人体的哪个方向；
  - 后 3 个数：照片里"向下"这个方向，指向人体的哪个方向。
  它们本身是单位向量（方向余弦），所以只表达"朝向"，不含长度。

- **法向量 = 两张照片之间"堆叠"的方向** = 照片平面的垂直方向。
  由"向右方向 × 向下方向"叉积得到，也就是垂直于照片、一层层往上的方向。

**最直观的比喻**：一叠打印纸从下往上摞着。
- 每张纸告诉你它左上角在房间的坐标（位置）；
- 每张纸还告诉你它是怎么摆放的（朝向）；
- 由朝向能算出"摞纸的方向"（法向量）；
- 把每张纸的位置往这个方向做投影，得到一个"高度值"，按高度从低到高排，就是正确的切片顺序。

**举个标准轴位 CT 的例子**：朝向为 row=(1,0,0)（向右指向患者左侧）、col=(0,1,0)（向下指向患者后方），叉积得 normal=(0,0,1)（往头顶方向摞）。各层位置 z 依次是 -100、-95、-90……投影值就是 z 本身，按 z 排就是从脚到头。层间距 5mm 也就是相邻 z 的差。

**为什么要这么麻烦**：因为光知道每张照片的坐标还不够——如果照片不是水平摆的（斜切、冠状、矢状采集），单看某个轴的大小可能反而排错；只有沿"真正垂直照片的方向"投影，才能得到唯一、正确的顺序，并顺带算出真实层间距、发现丢层或重复层。

**为什么值是三个负数**：

1. **只有相对位置有意义，绝对数值是任意的**。DICOM 患者坐标系的原点并不强制固定在某个解剖点，而是由设备厂商/扫描协议决定（常见取机架等中心 isocenter、扫描床某点或图像中心附近）。所以 (0,0,0) 只是一个人为基准点，不代表人体某个固定位置。
2. **LPS 轴方向决定符号**：+X = 患者左手方向，+Y = 患者后方，+Z = 患者头侧。若某张切片落在原点的"右侧/前方/脚侧"，对应分量就是负数。
3. 因此三个负数很常见：原点设在机架等中心时，扫描部位若整体位于基准的右侧、前方或脚侧，三个分量就可能全为负。它只表示"在基准点的哪一侧"，没有特殊含义。
4. 重建/MPR 只关心**相对差值（层间距）**，所以正负号不影响结果，只要排序和间距算对即可。
5. 补充：有时也会看到 (0,0,0) 附近的小值或全为正，取决于设备；同一个序列的切片位置通常沿着法向量单调递增或递减，符号规律是一致的。

**常见误区**：

- 排序是"**分组之后、每套序列内部**"进行的，不是对所有混在一起的切片做全局排序。先按 `SeriesInstanceUID` 分桶，再对桶内切片按投影值排序。
- 排出来的是"**空间/几何顺序**（物理层叠顺序）"，**不等于"扫描时间顺序"**。MPR 需要的是空间顺序（谁挨着谁、间距多少）才能正确堆叠体数据；采集时间顺序可能不同（如螺旋 CT 重建、隔层采集、重建顺序与采集顺序不一致）。标准轴位平扫时二者通常一致，所以容易混淆。
- 排序方向（脚→头 或 头→脚）取决于法向量符号，只要整个序列方向一致即可，不影响重建结果。

**法向量从哪来（关键澄清）**：法向量**不是**由 ImagePositionPatient 得到的，而是来自 `ImageOrientationPatient (0020,0037)`。

- `ImagePositionPatient` 是一个"**点**"（位置向量），回答"这张切片左上角在哪"，本身不含朝向信息。
- `ImageOrientationPatient` 是"**两个方向**"（单位向量）：前 3 个数为行方向 row，后 3 个数为列方向 col，均为方向余弦、不含位置。
- **法向量 = row × col（叉积）**。叉积天然垂直于 row 和 col，即垂直于整张切片平面、指向层叠方向。同一序列所有切片共享同一个法向量。
- 得到法向量后，才把每个 ImagePositionPatient（点）与法向量做**点积**，降维成一个标量：`d = ImagePositionPatient · normal`。

流程：**朝向 tag 定法向量 → 位置 tag 投影得排序键**。

- 理论上也可用"相邻切片位置之差"估计层叠方向，但遇到丢层/重复层就不准；正规做法用叉积，位置差只用于校验层间距。

**ImagePositionPatient 是左上角，不是中心**：

- DICOM 定义 `ImagePositionPatient (0020,0032)` 为"**左上角（第一个被传输体素，即 row=0、col=0）的中心**"在患者坐标系中的坐标，**不是切片中心**。
- 切片中心需自行计算（以 4×4、像素间距 1mm、标准朝向、位置 (0,0,0) 为例）：

  ```
  center = P0
         + X方向 × ((列数-1)/2) × 列间距    // X方向 = ImageOrientationPatient 前 3 个
         + Y方向 × ((行数-1)/2) × 行间距    // Y方向 = 后 3 个
  ```

  得 center = (0,0,0) + (1,0,0)×1.5 + (0,1,0)×1.5 = **(1.5, 1.5, 0)**。
- 奇数尺寸时体素中心恰好落在某个体素上；偶数尺寸时中心落在体素之间。

**形象理解**：一张切片可以看成一个"**扁平的长方体，由许多小长方体（体素）拼成**"；把切片一层层叠起来，就是整个三维体数据。`ImagePositionPatient` 是其中**左上角体素的中心坐标**（注意是中心，不是方块的角），它锚定了整张切片的位置；再配合朝向 + 像素间距 + 行列数，整张切片的空间位置就完全确定。小方块通常是"小长方体"而非正方体（行/列间距与深度往往不同）。

### A3 ★ 建立 IJK↔LPS 仿射矩阵（Orientation/Position/PixelSpacing）

**说明**：

**两个坐标系**
- **IJK（图像坐标系）**：体素索引 `(i, j, k)`，i = 列号（向右增）、j = 行号（向下增）、k = 层号。整数，单位是"格"。
- **LPS（患者坐标系）**：`(x, y, z)`，单位 mm，+X 左、+Y 后、+Z 上。

**通俗理解**

- **图像坐标系（IJK）** 描述一个体素"在图像网格里的序号/格子位置"，单位是格（pixel/voxel），**不是物理长度**；只回答"第几行、第几列、第几层"，跟真实空间无关。类比：Excel 的单元格地址（第 3 行第 5 列）。
- **患者坐标系（LPS）** 描述"人体内某点在真实三维空间的位置"，单位 mm，方向由解剖方向固定（X 右→左、Y 前→后、Z 下→上）；与某次具体扫描无关，只要体位一致，同一解剖点坐标一致；原点由设备定义（人为基准，故可为负）。类比：房间/地图上的平面坐标或经纬度。

| | 图像坐标系 (IJK) | 患者坐标系 (LPS) |
| --- | --- | --- |
| 单位 | 格 / 体素 | 毫米 mm |
| 原点 | 图像第一体素（左上角） | 设备自定基准 |
| 方向 | i 右、j 下、k 后 | X 左、Y 后、Z 上 |
| 意义 | 数组下标 | 真实解剖空间 |
| 用途 | 访问数组、插值 | 显示/测量/配准/融合 |

程序里访问体素用 IJK（数组下标），测量、配准、MPR 定位在 LPS 里算，故需 4×4 仿射矩阵互译。补充：RAS 只是把 LPS 前两轴取反，是 NIfTI / 3D Slicer 的内部约定。

**目标**：把索引 `(i, j, k)` 映射到患者坐标 `(x, y, z)`。有了它（及其逆），MPR 才能在任意世界坐标点上取体素值。

**三个零件刚好够用**

| 零件 | 提供 |
| --- | --- |
| `ImageOrientationPatient` 前 3 个 | X 方向（列号增大方向，单位向量） |
| `ImageOrientationPatient` 后 3 个 | Y 方向（行号增大方向，单位向量） |
| Z 方向 = X × Y | 垂直于切片、层叠方向（单位向量） |
| `PixelSpacing` | Δi = 列间距，Δj = 行间距（注意顺序！） |
| 层间距 Δk | 相邻切片位置沿 Z 的差值 |
| `ImagePositionPatient` | P0 = 索引 (0,0,0) 体素的中心 |

**核心公式**（体素索引 → 患者坐标）

```
P(i,j,k) = P0 + i·Δi·X + j·Δj·Y + k·Δk·Z
```

从左上角体素出发，往列方向走 `i` 格、往行方向走 `j` 格、往层方向走 `k` 格。

**写成 4×4 齐次矩阵**（前三列是"带间距的方向向量"，第四列是平移）

```
         ┌ Xx·Δi  Yx·Δj  Zx·Δk  P0x ┐   ┌ i ┐
patient  │ Xy·Δi  Yy·Δj  Zy·Δk  P0y │ · │ j │
         │ Xz·Δi  Yz·Δj  Zz·Δk  P0z │   │ k │
         │   0      0      0     1  │   └ 1 ┘
         └                          ┘
```

**逆矩阵用于"患者坐标 → 索引"**：MPR 时对每个输出像素，先算它在世界坐标的位置，再用逆矩阵转成（可能带小数的）`(i,j,k)`，然后插值取灰度值。

**与 VTK 的对应**：VTK 的 `vtkImageData` 把这三样分开存，等价于本矩阵（`IndexToPhysicalMatrix = direction · diag(spacing) + origin`）：

```java
imageData.SetOrigin(P0x, P0y, P0z);          // ImagePositionPatient
imageData.SetSpacing(dx, dy, dz);            // PixelSpacing + 层间距
imageData.SetDirectionMatrix(Xx,Xy,Xz, ...); // Orientation 构成的 3×3
```

**易错点**
1. **`PixelSpacing` 顺序是 `[行间距, 列间距]`**，别和 i/j 搞反（最常见 bug）。
2. 方向余弦应是**正交单位向量**；厂商数据可能有微小误差，必要时正交化。
3. **Δk 的符号**：切片按头→脚排序时 Δk 为负，只要与排序一致即可；也可取绝对值统一。
4. 序列内朝向不一致时，**单个仿射矩阵不够**，需拆分或重采样。

**一句话**：这个 4×4 矩阵是"从索引格子到毫米空间"的翻译器——正着用是显示/定位，反着用是 MPR 采样，是贯穿整个工具的核心数据结构。

**形象理解与精度要点**

- 一张切片 = n 个格子平铺；多层堆叠 = 一个三维格子盒（体数据），它是对人体某段区域的**离散采样**表示。
- 患者坐标系里的点是**连续**的，用逆矩阵换算得到的是**连续索引 `(i, j, k)`，常带小数**：落在格子中心即该体素；更多时候落在格子之间，需取邻近格子**插值**（三线性等）。
- 盒子只覆盖**扫描视野（FOV）**，人体超出盒子则无法表示（换算越界）。
- 各方向尺寸不一定相等（格子非正方体），严格说是"长方体"。

**更精确的表述**：患者体内的一个连续点，映射到格子盒里的某个（可能非整数的）位置，用周围格子插值出该点的值。

**一句话确认**：IJK↔LPS 就是把"格子地址"和"格心在人体的毫米坐标"互相换算——整数 `(i,j,k)` 对应格心，带小数的对应格间点（需插值）；格→人体用于显示/定位，人体→格用于 MPR 采样。

**"IJK 端"与"LPS 端"的数据依据**

这不是两个矩阵，而是**同一个仿射矩阵的两端**：

- **IJK 端（输入 = 索引）** 依据"数组本身"：像素数据数组 + 维度信息 `Rows (0028,0010)`、`Columns (0028,0011)`、层数/帧数。它只决定"格子有多少、索引范围 0..(n-1)"，**不含物理位置信息**。像素灰度值只是强度，不参与建矩阵。
- **LPS 端（输出 = 物理位置）** 依据 DICOM 空间几何标签：

  | 矩阵成分 | 依据的 tag |
  | --- | --- |
  | 平移 P0 | `ImagePositionPatient (0020,0032)` |
  | 3×3 方向 | `ImageOrientationPatient (0020,0037)` |
  | X/Y 缩放 | `PixelSpacing (0028,0030)` |
  | Z 缩放 | 层间距：位置差 / `SpacingBetweenSlices (0018,0088)` / `SliceThickness (0018,0050)` 回退 |

- 补充：若数据没有这些 tag（如把普通 PNG/JPG 当序列），就没有真实 LPS 信息，只能人为假设标准朝向与平移（如当作轴位、原点置 0），在程序内自定义坐标系。

**通俗版（这一步的作用）**

比喻：把体数据想成一本"用格子记账的书"——你只看到"第 k 页、第 j 行、第 i 格"里写着什么（灰度值），却完全不知道这些格子在人体哪个位置、多大、朝哪个方向。DICOM 几何标签就是这本书的"出版信息"：从哪开始排（Position）、朝哪个方向排（Orientation）、每格多大（PixelSpacing）、每页多厚（层间距）。**仿射矩阵就是把这些格子精确贴到人体地图上的换算表。**

它解决的就一件事：**索引格子 ↔ 真实毫米空间 的互相换算**。

作用（为什么必须做这一步）：
1. **知道像素在人体哪儿、多大**：否则 MPR 方向可能错、左右可能反，甚至不知切的是哪个部位。
2. **MPR 核心采样**：给定任意切面，需反算切面上每个点对应哪个格子才能取灰度值——靠**逆矩阵**。
3. **三视图联动**：三个视图必须在同一"人体坐标点"上对齐。
4. **测量真实尺寸**：量线长是否 mm，必须用世界坐标算，不能数格子。
5. **多模态融合**：CT 与 PET 的格子要贴到同一人体坐标系才能叠加。
6. 后续定位、标注、导出、手术导航等都建立在它之上。

一句话：**它把"一堆没有空间概念的像素"变成"一张贴在人体真实尺寸和方位上的地图"**；没有这一步，MPR 无从谈起。

### A4 ★ Rescale Slope/Intercept 还原 HU 真实值

**说明**：

**问题**：DICOM 里存的像素值（stored value）是**整数**（Uint16/Int16），是设备加工过的刻度，**不是物理量**。

**还原公式**（逐像素线性变换）：

```
HU = stored_value × RescaleSlope + RescaleIntercept
```

| tag | 名称 | 含义 |
| --- | --- | --- |
| (0028,1053) | RescaleSlope | 斜率（放大倍数），CT 通常 = 1 |
| (0028,1052) | RescaleIntercept | 截距（零点偏移），CT 通常 = −1024 |

**比喻**：stored 是被压缩/平移过的尺子刻度；斜率是放大倍数，截距是零点偏移；换算后对齐到 CT 通用刻度 HU。

**HU（Hounsfield Unit）参考值**：空气 ≈ −1000，水 = 0，脂肪 ≈ −100~−50，软组织/肌肉 ≈ +40~+80，骨 ≈ +1000 以上。

**举例**（slope=1, intercept=−1024）：stored=1000 → HU=−24（近水）；stored=2048 → HU=1024（骨）。

**为什么必须做**
1. 窗宽窗位预设（肺窗/骨窗等）按 HU 定义，不还原会错乱；
2. HU 阈值分割、ROI 统计都基于 HU；
3. 不同厂家原始存储值不同，转 HU 后才可跨设备比较。

**注意点**
- 有的图像用 `ModalityLUTSequence`（查找表）而非线性公式；更高级用 `RealWorldValueMapping`；
- MR 无 HU 这类统一物理单位，rescale 后仍是任意单位，不能套 CT 预设；
- slope=1、intercept=0 时无需变换；
- 变换是逐像素的，应在读取/显示前完成。

**与窗宽窗位的关系（易混）**：这是两个不同目的的变换，顺序不能反。

```
原始存储值 (stored)  ──①Rescale──▶  真实值 HU  ──②窗宽窗位──▶  显示灰度 (0~255)
```

- **① Rescale（Modality LUT）** 得到**真实物理值 HU**，与显示无关。
- **② 窗宽窗位（VOI LUT）** 把 HU 压缩/拉伸成显示器灰度，近似 `display = 255 × (HU − (WL − WW/2)) / WW`，再截断到 0~255；WW=窗宽（映射的 HU 范围）、WL=窗位（范围中心）。窗外值截断为全黑/全白。
- 因此**窗宽窗位是可视化手段，不是求真实值的手段**；测量、阈值、ROI 统计用的都是 HU，而非显示灰度。

### A5 ★ 处理压缩传输语法（RLE/JPEG/JPEG2000，经 GDCM）

**说明**：DICOM 的 **Transfer Syntax (0002,0010)** 规定像素数据怎么编码，解码后才能拿到真正的体素值。

- **未压缩**：Implicit/Explicit VR Little Endian（最常见）、Big Endian（少见）。
- **压缩**：**RLE**（无损，DICOM 自定义行程编码）；**JPEG Baseline**（有损）、**JPEG Lossless**；**JPEG-LS**（近无损/无损）；**JPEG 2000**（无损或有损，医学常用）；**Deflated**（整文件 zlib）。
- "处理"要做：**识别传输语法 → 解码成原始像素数组 → 处理多帧**。不解码就无法取体素值。
- **Java 落地**：`dcm4che` 是纯 Java 首选，同时支持 DIMSE + DICOMweb + 多种压缩；也可用 GDCM 的 JNI 绑定；JPEG2000 可用 openjpeg/jj2000 的 Java 封装。
- **注意**：JPEG Baseline 是**有损**的，可能影响测量精度；诊断数据优先无损（RLE/JPEG-LS/JPEG2000 无损）。

### A6 ☆ 多模态支持（CT/MR/US）、多序列并存、缺失切片/层间距不一致告警

**说明**：

- **多模态**：`Modality (0008,0060)` 标识 CT/MR/US/PET 等，语义不同：CT 有 HU 绝对值；MR 无统一物理单位；US 通常是二维实时、几何随探头变化，**一般不适合 MPR**；PET 有 SUV 概念。MPR 主要面向断层容积数据（CT/MR/CBCT/PET）。
- **多序列并存**：一次检查常含多个序列（MR 的 T1/T2、CT 的平扫/增强）。程序要能列出、让用户选择加载，并管理内存（大序列同时加载易爆内存）。
- **缺失切片 / 层间距不一致告警**：按位置排序后检查相邻层间距是否与预期一致——间距异常大=可能丢层；间距忽大忽小=采集异常；重复层=文件重复。这些会让 MPR/插值结果错误，必须**提示用户**而非静默继续（健壮性要求）。

### A7 ☆ 本地文件 / DICOMweb(WADO-RS) / PACS(DIMSE C-GET) 接入

**说明**：三种数据来源，对应不同集成场景。

| 来源 | 机制 | 场景 |
| --- | --- | --- |
| 本地文件 | 直接读磁盘/光盘/U 盘目录 | 离线阅片、导入导出 |
| DICOMweb (WADO-RS) | HTTP REST：QIDO-RS 查询、WADO-RS 取图、STOW-RS 上传 | 现代/云 PACS、Web 集成 |
| PACS (DIMSE) | DICOM 传统网络协议（TCP）：C-FIND 查询、C-GET/C-MOVE 取图 | 医院旧式 PACS |

- **DICOMweb** 是 RESTful 标准，走 80/443，易穿防火墙，适合 Web/云。
- **DIMSE** 是传统二进制协议（默认端口 104/11112），需配置 AE Title、SCU/SCP 角色，内网 PACS 常用。
- **Java 落地**：`dcm4che` 一套库同时支持 DIMSE 与 DICOMweb（QIDO/WADO/STOW）。
- 这一项是"接入能力"：让工具能连医院 PACS 直接拉片，而不只是读本地文件。

---

## B. 体数据构建与预处理

### B1 ★ 由序列构建 vtkImageData 三维体数据

**说明**：`vtkImageData` 是 VTK 的**规则三维网格**（结构化点集），属性为**尺寸 Dimensions、原点 Origin、间距 Spacing、方向矩阵 Direction**，外加一段连续体素数组。

从 DICOM 序列构建的步骤：

1. **分组排序**（见 A2）：得到同一序列、按物理顺序排好的切片。
2. **确定尺寸**：`nx = Columns`、`ny = Rows`、`nz = 切片数`。
3. **设置几何**（对应 A3 的仿射矩阵：origin + spacing + direction）：

   ```java
   imageData.SetDimensions(nx, ny, nz);
   imageData.SetOrigin(P0);                 // 第一张切片的 ImagePositionPatient
   imageData.SetSpacing(dx, dy, dz);        // dx=列间距, dy=行间距, dz=层间距
   imageData.SetDirectionMatrix(Xx,Xy,Xz, Yx,Yy,Yz, Zx,Zy,Zz); // X,Y,Z 方向
   ```

4. **分配体素数组**：`AllocateScalars(type, comps)` 给体数据准备一段**连续内存**。
   - 内存大小 = `nx × ny × nz × 分量数 × 每分量字节`。
   - 数据类型按 DICOM 选：依据 `BitsAllocated (0028,0100)`、`BitsStored (0028,0101)`/HighBit、`PixelRepresentation (0028,0103)`（0=无符号、1=有符号）。CT 常 16 位有符号 → `VTK_SHORT`；MR 可能无符号 → `VTK_UNSIGNED_SHORT`。选错会导致数值错/溢出/内存浪费。
   - 分量数：灰度 = 1，彩色 RGB = 3。
   - Java：分配后用 `GetScalarPointer()` / `GetJavaArray()` 取可写数组。VTK 要求体素**连续存放**，后续 reslice/映射/体绘制都在该内存上运行。
5. **回填像素**：把每张切片像素拷到第 k 层。
   - VTK 数组**x 最快**（x→y→z），索引 `index = i + j·nx + k·nx·ny`（i=x列, j=y行, k=z层）。
   - DICOM 每张切片也是**行优先**：片内索引 `i + j·nx`，与 VTK 一致 → 一张切片可**整块拷贝**。
   - 第 k 层对应排序后第 k 张切片，起始偏移 `k·nx·ny`；**k=0 必须是 origin 所在那张**，故 `SetOrigin` 用排序后第一张的位置，否则层错位。
   - 用 `System.arraycopy` **批量**拷贝，避免逐像素 JNI 往返（极慢）。
   - 注意：多帧按帧重排；RGB/YBR 重排平面；位深/符号匹配数组类型；字节序与 VTK（小端）一致。
   - 拷入的是**存储值**，HU 换算可后续用 `vtkImageShiftScale`（slope/intercept）在管线里做。
   - **步长（stride）概念**：`index = i·1 + j·nx + k·(nx·ny)`。列 +1 格 → 偏移 +1；行 +1 格 → 偏移 +nx；层 +1 格 → 偏移 +nx·ny。"多一行/多一层"即加对应固定步长。这与 A3 仿射矩阵同构：内存地址用"格子步长"(1, nx, nx·ny)，物理位置用"毫米步长"(Δi·X, Δj·Y, Δk·Z)。
6. **（可选）应用 rescale** 得到 HU，或用 `vtkImageShiftScale` 在管线里做。

构建完成后，`vtkImageData` 即 **`vtkImageReslice`（MPR）**、各种 Mapper、体绘制的统一输入。**关键前提是内存连续**，而 DICOM 是分散文件，这一步就是"把散片拼成一块连续体数据"。

### B2 ★ 体数据缓存与内存管理（大序列懒加载/流式）

**说明**：**问题**——体数据很大，内存 ≈ `nx × ny × nz × 每体素字节`：512×512×500×2 ≈ 262 MB；512×512×1500×2 ≈ 786 MB；多序列同时驻留可达数 GB。重复构建代价高，故需缓存与内存管理。

**常用策略**
- **按需构建 + 构建后缓存**：选定序列才构建 `vtkImageData`，构建一次后复用，**不要每帧重建**。
- **懒加载 / 流式**：随滚动只解码需要的切片；或先加载首尾/中间少数层做预览。
- **降采样预览**：交互中显示低分辨率，空闲后切全分辨率（`vtkImageResample`）。
- **LRU 淘汰**：内存只保留最近使用的几个序列，其余释放。
- **磁盘压缩常驻**：原始压缩数据留磁盘，用时再解码。

**Java + VTK 特别注意**
- `vtkImageData` 像素数组在 **VTK native 内存**（非 JVM 堆），要同时关注 JVM 堆与 native 内存，避免 OOM。
- 不用的 VTK 对象要主动 `Delete()`/释放，防 native 泄漏（GC 管不到）。
- 减少逐像素的 Java↔native 往返，尽量在 VTK 管线内完成。

**一句话**：B1 负责"把散片拼成一块体数据"，B2 负责"拼得别太多、别太久、用完及时放"。

### B3 ☆ 等体素重采样（消除斜切阶梯伪影）

**说明**：

- **问题**：CT 体素常为**各向异性**（如层内 0.6×0.6 mm、层间距 2.5 mm），z 方向采样稀疏。正交切面不明显，但**斜切**时切面穿过不同层体素，z 方向"一步跨很远"，边缘出现**阶梯/锯齿（stair-step）伪影**，测量失真。
- **做法**：把体数据**重采样成各向同性（等体素）**网格（三方向间距相同，如 0.6³ mm），用三线性/三次插值生成中间层；切面任意角度采样均匀，边缘平滑。
  - 实现：VTK 用 `vtkImageResample`，或 `vtkImageReslice` 指定等间距输出；ITK 用 `ResampleImageFilter`。
- **代价**：z 从 2.5→0.6 约**插值出 4 倍层数**，内存上升，且有一次性计算开销。
- **权衡**：可加载后统一做，也可仅在精细斜切 MPR 时按目标分辨率做。
- **注意**：插值只**平滑显示**，**不能创造真实分辨率**（2.5 mm 层间距的真实信息不会变丰富）。

### B4 ☆ 多帧/4D（时间序列）、PET-CT 融合配准

**说明**：

- **多帧**：单文件含多帧（`NumberOfFrames (0028,0008)`），如增强 CT 多时相、心脏电影、DSA。需拆成多个 3D 体数据，或加时间维 T。
- **4D**：3D 空间 + 时间轴（心脏、灌注、功能 MR），界面上需**时间轴播放/逐帧导航**并配合 MPR。
- **PET-CT 融合配准**：
  - PET 与 CT 分别采集，需**空间对齐后叠加**：CT 灰度解剖 + PET 伪彩，标示代谢活跃区；MPR 三视图同样融合。
  - **同机 PET/CT**：设备通常统一坐标（LPS 位置对齐），可直接用各自仿射矩阵叠加（融合）。
  - **不同设备/时间**：坐标系不一致，需**刚性/非刚性配准**后再融合。
  - 要点：不同 spacing/origin/dimension → 先重采样到**同一网格**；PET 做 **SUV 归一化**与伪彩映射；VTK 用 `vtkImageBlend` 或多 mapper 叠加。

**单机 CT 场景的取舍**：若只处理同一台 CT、同一模态的数据——
- **PET-CT 融合配准**：不需要（同一坐标系）。
- **4D**：常规静态 CT 不需要；但心脏 CT（多心动周期）、CT 灌注、动态增强仍是 4D，需要时间轴。
- **多帧**：取决于**导出格式**，与是否单机无关。现代 CT 常用 **Enhanced CT（多帧对象）**（一个文件含 `NumberOfFrames` 帧）；若为传统"一文件一层"则不涉及。建议 MVP 保留多帧解析（成本低、数据层健壮性），上线前用目标设备样本确认导出格式。

---

## C. MPR 渲染核心

### C1 ★ 横断/冠状/矢状三正交视图（vtkImageReslice）

**说明**：

**目标**：从三维体数据切出三个互相垂直的平面并分别显示——MPR 的"三视图"。

**为什么用 `vtkImageReslice`**
- 它是 VTK 中**从体数据提取任意平面**的核心过滤器（A3 仿射矩阵的"应用版"）。输入 `vtkImageData`，输出一张二维切片。
- 关键设置：

  ```java
  reslice.SetInputData(imageData);
  reslice.SetOutputDimensionality(2);        // 输出二维切片
  reslice.SetResliceAxes(axes);              // 4×4 矩阵：定义切哪个平面
  reslice.SetInterpolationModeToLinear();    // 插值方式（见 C4）
  reslice.SetOutputSpacing(dx, dy, 1);       // 输出像素间距
  ```

- **`SetResliceAxes`（4×4）就是切面的坐标系**（与 A3 同构）：前 3 列 = 输出平面的 X、Y、法线方向；第 4 列 = 平面通过的原点（移动它 = 翻层/滚动）。也可用 `SetResliceAxesDirectionCosines(...)` + `SetResliceAxesOrigin(x,y,z)` 组合。

**三个视图怎么设**

| 视图 | 平面法线 | 输出平面 x/y 方向 |
| --- | --- | --- |
| 横断面 Axial | 沿 Z | x=体数据 x, y=体数据 y |
| 冠状面 Coronal | 沿 Y | x=体数据 x, y=体数据 z |
| 矢状面 Sagittal | 沿 X | x=体数据 y, y=体数据 z |

**典型渲染管线**

```
DICOM → vtkImageData
          ├─ vtkImageReslice(Axial)   → vtkImageMapToWindowLevelColors → vtkImageActor → Renderer
          ├─ vtkImageReslice(Coronal) → ... → Renderer
          └─ vtkImageReslice(Sagittal)→ ... → Renderer
                                              └─ 同一个 vtkRenderWindow（3 个视口/窗格）
```

- 窗宽窗位可用 `vtkImageMapToWindowLevelColors` 加在 reslice 之后。
- 只有"当前切面"会被计算（输出是 2D），**翻层很快**，无需重算整个体数据。

**优点**：比 `vtkImageViewer2`/`vtkResliceImageViewer` 更底层灵活，能直接支持**斜切**（旋转 axes），也是实现三视图联动的正确基础（见 C2）。

### C2 ★ 三视图十字线联动（vtkResliceCursor + Widget）

**说明**：

**目标**：三个视图显示同一解剖位置——在任一视图拖动十字线，另外两个视图立刻跳到对应层；滚动/旋转也同步。

**核心思路：一个共享的"游标"状态**
- `vtkResliceCursor`：保存三个平面的**共享状态**——一个中心点（世界坐标）+ 三个平面各自的位置/朝向，是三视图的**唯一真相源**。
- `vtkResliceCursorWidget`：交互部件，把鼠标拖动转成对 cursor 的更新。
- `vtkResliceCursorLineRepresentation`：绘制十字线（两条线分别是另外两个平面的投影）并处理交互。

**连线方式（关键）**：三个 representation **共享同一个 cursor**，任意一个被拖动其余自动跟随 → 天然联动。

```java
repAxial.GetResliceCursorActor()
        .GetCursorAlgorithm()
        .SetResliceCursor(cursor);   // Sagittal / Coronal 同样指向该 cursor
```

**设置三个平面朝向**

```java
repAxial.SetPlaneOrientationToZAxis();     // 横断
repSagittal.SetPlaneOrientationToXAxis();  // 矢状
repCoronal.SetPlaneOrientationToYAxis();   // 冠状
```

**典型搭建步骤**：建 cursor（`SetCenter`、`SetImage`）→ 建 3 renderer + 3 widget + 3 representation → 三者都 `SetResliceCursor(cursor)` → 设朝向（Z/X/Y）→ `widget.SetRepresentation(rep); widget.SetInteractor(interactor); widget.EnabledOn();` → `renderWindow.AddRenderer(...)`、`Render()`。

**与 C1 的关系**：cursor 的三个平面本质就是 C1 中三个 `vtkImageReslice` 的 4×4 axes；Widget 内部用 cursor 完成 reslice 并绘制。也可**自研**：维护一个公共 4×4 矩阵，手动处理鼠标事件更新它再重算三份 reslice——更可控但工作量大，多数自研项目走这条路。

**补充**：中心点是**世界坐标（LPS）**→ 三视图对应同一解剖点；支持**旋转（斜切）**与**厚层**（`cursor.SetThickMode(1)` + 厚度）；VTK 自带 widget 开箱即用但定制性一般，生产应用常在其上做自定义交互。

### C3 ★ 任意斜切（oblique）

- **状态**：**已实现（S0–S8 完成，离屏验收全 PASS；待 GUI 手测确认）**。行为规范与验收见 **`MPR斜切设计.md`**，代码入口见 `MPR业务流程.md` ⑯。
  - 离屏证据：`M3ResliceCheck`（§4 三表逐条断言、交点屏幕位移 0.000000、十字线平行屏幕轴、被转视图相机冻结、十字线转 90.00°、3 张 PNG）；`M3RigCheck`（累积旋转下 C 钉住/相机冻结/正对平面）；`M0ResliceSmoke`（显示管线与三线性采样逐像素一致）。
- **行为**：三平面永远两两正交、共交于中心点 C；在任一视图**顺时针拖十字线** = 绕该视图视线方向旋转（角度无限制）；**被转视图**的平面/相机/图像完全不变，只有十字线在屏内转动；**另两个视图**的十字线与中心点屏幕位置**严格不变**（仍是屏幕对齐的"＋"），显示内容变为斜切采样面。
- **规则**：`up` 采用**连续性**（由光标坐标系携带）+ 一次性"**回正**"（`A`/按钮）；滚轮沿平面法向翻层；`R` 重置；状态栏显示"沿法向偏移 mm + 斜切角"。
- **读数**：HU 直接在**原始体数据**上按世界坐标三线性采样，斜切下依然准确；**斜切下测量暂不可用**（禁用并提示）。
- **实现**：每视图一套 `vtkImageResliceMapper` + `vtkPlane`（实时，不重采样整卷）；几何由 `geometry/MprCursorFrame`（纯数学、可单测）承担。
- **90° 变化对照表**：见 `MPR斜切设计.md` §4。

### C4 ★ 插值方式选择（最近邻/三线性/三次）

### C5 ☆ 厚层 MPR（Slab 厚度可调）

> 实现提示：`vtkImageResliceMapper` 自带 `SetSlabThickness` + `SetSlabTypeToMean`（均值即厚层）、`SetSlabSampleFactor`，可与 C3 的斜切平面叠加。

### C6 ☆ MIP / MinIP / AvIP 投影

> 实现提示：同上，`SetSlabTypeToMax`（MIP）/ `ToMin`（MinIP）/ `ToMean`（AvIP），厚度即投影层厚。

### C7 ☆ CPR 曲面重建、参考线、方向标记（orientation marker）

---

## D. 交互

### D1 ★ 鼠标滚轮翻层、平移、缩放

### D2 ★ 十字线拖拽平移/旋转（旋转 plane）

### D3 ★ 窗宽窗位调节、重置

### D4 ★ 在任一视图定位后其余两视图同步跳转

### D5 ☆ 鼠标功能绑定预设/键盘快捷键

### D6 ☆ 光标位置与 HU 值实时读数

---

## E. 显示与窗宽窗位

### E1 ★ 窗宽窗位预设（肺/骨/软组织/脑等）

### E2 ★ 灰度映射、反色、物理值标尺

### E3 ★ 叠加信息（患者/序列/层位/层厚/窗值）

### E4 ☆ 伪彩/LUT、VOI LUT、DICOM Presentation State(GSPS)

---

## F. 测量与标注

### F1 ★ 距离、角度、矩形/椭圆 ROI

### F2 ★ ROI 内 HU 统计（均值/最大/最小/标准差）

### F3 ☆ 探针、文本/箭头标注、自由多边形 ROI

### F4 ☆ 标注保存/加载（自定义或 DICOM SR）

---

## G. 3D 与高级后处理

### G1 ☆ 体绘制 VR（vtkGPUVolumeRayCastMapper）+ 传输函数

### G2 ☆ 面绘制（Marching Cubes）、3D 裁剪/切割

### G3 ☆ 分割结果叠加、图像滤波/增强

---

## H. I/O 与导出

### H1 ★ 导出当前 MPR 视图为图片（PNG/JPEG/BMP）

### H2 ☆ DICOM 二次捕获(SC)/视频录制、测量报告导出、打印

---

## I. 系统与非功能

### I1 ★ 布局管理（1×3、2×2、含 3D 的四视图）

### I2 ★ 异常处理与日志（log4j）、线程模型

### I3 ☆ 配置持久化、国际化、多屏/HiDPI

### I4 ☆ 性能：GPU 纹理、多线程、降采样预览

---

## J. Java + VTK 特有工程点

### J1 VTK Java 绑定（vtkJava）通过 JNI 桥接，注意 JVM Native 内存回收与 GC

### J2 GUI 用 Swing（vtkCanvas，AWT）或 JavaFX 嵌入渲染窗口的方案选型

### J3 DICOM 读取建议 GDCM/DCMTK；ITK Java 绑定较弱，图像处理需评估

### J4 Maven/Gradle 打包与各平台 native 库分发

---

## 附录：术语表（Glossary）

- **解剖方位缩写（方向标记 C7）**：均为"相对患者本人"的方位——
  **A** = Anterior 前（腹侧）、**P** = Posterior 后（背侧）、**L** = Left 患者左、**R** = Right 患者右、
  **H** = Head 头侧（=上/Superior）、**F** = Feet 脚侧（=下/Inferior）。
  各视图使用：轴位 = A/P + L/R；冠状 = H/F + L/R；矢状 = H/F + A/P。
  注意 **L/R 是患者本人的左右**，不是屏幕左右；按放射科约定"患者右显示在图像左侧"，与"从脚往头看"为同一件事。

- **像素 Pixel**：二维图像的最小方格，只有宽、高。
- **体素 Voxel（Volume Element）**：三维体数据的最小方块，比像素多一个"深度/厚度"维度，内部保存一个灰度/密度值。
- **体数据 Volume**：由一层层切片叠成、由体素构成的三维网格（三维标量场）。
- **各向同性 / 各向异性**：体素在三个方向上尺寸相等为各向同性（立方体），不等为各向异性（如 0.6×0.6×2.5 mm）。各向异性会导致斜切出现"阶梯"伪影。
- **层厚 vs 层间距（体素深度）**：`SliceThickness (0018,0050)` 是采集层面板的**物理厚度**（描述"一刀多厚"，相邻层可重叠）；体素在深度方向的尺寸应取**层间距**（相邻 ImagePositionPatient 沿法向的差值，即采样间隔）。无缝隙采集时二者相等；重叠/有间隙时不等。取值优先级：位置算出的层间距 > `SpacingBetweenSlices (0018,0088)` > `SliceThickness`（单层回退）。
- **一张切片在深度方向只含一层体素**：体素网格按采样点（层中心）排列，一张切片提供一个采样层，故深度方向只有一个小方块厚（一个 k 值）。层厚大于层间距时物理层面板重叠，但重叠是信息冗余/部分容积，不会变成体数据里额外的体素层。若做 z 方向细重采样可插值出更多层，但那是插值结果，非原始采样。
- **重叠的条件是"层厚 > 层间距"，不是只看层厚**：两种模型要分清。
  - 采集足迹模型（每刀实际扫到的物理板）：重叠条件为 `层厚 > 层间距`；层厚 =1、间距 =1 无重叠；层厚 =1.5、间距 =1 重叠 0.5；层厚 =1.5、间距 =1.5 反而无重叠。
  - 体素网格模型（体数据里的方块）：方块厚度 = **层间距**，方格一格挨一格、**永不重叠**。
  - 例（深度 2 的物体）：层厚 1、间距 1，中心 0.5/1.5 → 覆盖 [0,1] 与 [1,2]，正好铺满不重叠；层厚 1.5、间距 1，中心 0.5/1.5 → 采集足迹重叠 0.5，但体数据仍是 2 层、每层厚 1。
  - 结论：**采集足迹是否重叠 = 比较层厚与层间距；体素网格永远按层间距排布且不重叠。**
- **HU（Hounsfield Unit）**：CT 值单位，由像素值经 Rescale Slope/Intercept 线性映射得到。
- **重采样 Resampling**：把体数据按新网格重新取样的过程，MPR 的核心操作。
- **插值 Interpolation**：重采样点不落在体素中心时，由相邻体素估算其值（最近邻/三线性/三次等）。
- **IJK**：图像/体素索引坐标系，单位 pixel/voxel。
- **LPS / RAS**：解剖（患者）坐标系，单位 mm；LPS 为 DICOM 标准，RAS 与之前两轴符号相反（NIfTI、3D Slicer 内部使用）。
- **法向量 Normal**：由 ImageOrientationPatient 的行、列方向叉积得到，垂直于切片平面、指向层叠方向。
- **同一序列法向量一般相同**：标准横断/冠状/矢状序列所有切片共享同一 ImageOrientationPatient，故法向量一致（这是能直接叠成体数据的前提）。但 DICOM 不强制：定位像、部分重建/心脏电影序列、厂商数值舍入等可能导致朝向不同。工程上应校验一致性（允许小容差，如方向余弦点积接近 1），差异大则拆分序列或另做重采样/配准；朝向非平行的序列本身不是规整体数据，不能直接堆叠。
- **MPR**：多平面重建，沿任意平面重采样出二维切片。
- **MIP / MinIP / AvIP**：最大 / 最小 / 平均密度投影，沿视线方向对厚层体素取最大/最小/平均。
- **CPR**：曲面重建，沿中心线展开的曲面 MPR。

---

## 附录：问答解释索引

| 条目 | 主题 |
| --- | --- |
| A2 | SeriesInstanceUID 分组 + ImagePositionPatient 法向量排序（已解释） |
