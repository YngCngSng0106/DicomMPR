# MPR 斜切（C3）设计说明（讨论结论沉淀）

> 本文档记录 MPR 斜切功能的**行为规范**与**实施约定**，由需求讨论逐条确认后沉淀，供实现与验收对照。
> 软件结构入口见 `MPR业务流程.md`；功能条目状态见 `MPR功能清单.md`。

---

## 1. 范围与已定决策

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| D1 | 旋转时的取景 | **最终版**：拖动中**冻结取景**，**松手后继续保持冻结**（允许斜切面四角被裁）；只有显式操作才重新取景——`R` 重置、`A` 回正、改变窗口尺寸。中心点屏幕位置始终严格不变 |
| D2 | 斜切下的测量 | **暂不做**：frame 非轴对齐时禁用测量工具并在工具条/状态栏提示（斜切测量另开任务） |
| D3 | "回正"触发 | 工具条按钮 + 快捷键 `A`（候选集合 = {视线符号} × {up 取两轴之一、±}，共 8 种，均保持十字线为"＋"）**（待最终确认，暂按此项实现）** |
| D4 | 状态栏 | `序列 | 偏移 矢a/冠b/轴c mm | 斜切 矢x°/冠y°/轴z° | HU=… | 提示` |
| D5 | 滚轮（翻层） | 沿**该平面法向**按体素间距步进（斜切后即沿斜法向） |

**做**：光标坐标系、拖动旋转（实时）、拖动移动中心、滚轮翻层、斜切显示、HU 读数、方向标记、状态栏、回正/重置。
**不做**：斜切下的测量与 ROI 统计。

---

## 2. 坐标与方向约定

### 2.1 患者坐标（世界坐标，mm，DICOM LPS）

| 轴 | 正方向 | 负方向 |
| --- | --- | --- |
| X | 患者**左** L | 患者右 R |
| Y | 患者**后** P（背侧） | 患者前 A（腹侧） |
| Z | **头**侧 H | 脚侧 F |

本项目把 DICOM 的 origin/spacing/direction 直接写入 `vtkImageData`，因此 **VTK 世界坐标 = 患者坐标（mm）**。

体数据自身方向向量（`VolumeGeometry.axisX/Y/Z`）：

- `axisX` = DICOM 行方向 = 列索引 i 增大方向（配 `spacing[0]` = PixelSpacing[1]）
- `axisY` = DICOM 列方向 = 行索引 j 增大方向（配 `spacing[1]` = PixelSpacing[0]）
- `axisZ` = 层法向 = `axisX × axisY`（配 `spacing[2]` = 层间距）

标准轴位扫描时它们等于 X/Y/Z；斜位序列或反向扫描时不等，故方向标记一律由**实际向量**推导，不写死。

### 2.2 光标坐标系（cursor frame）

右手正交系 `(u, v, w)`，初始 `u = X`、`v = Y`、`w = Z`；三个平面分别为 `⊥u`（矢状面）、`⊥v`（冠状面）、`⊥w`（轴位面）。

- 每个平面用 `{x : (x − M)·n = offset}` 表示（M = 体中心，n = u/v/w，offset 单位 mm）；
- **三平面两两正交且唯一交于一点 C**：`C = M + a·u + b·v + c·w`（a/b/c = 三平面偏移）；
- 偏移与旧实现的索引语义一一对应：`a ↔ i`、`b ↔ j`、`c ↔ k`（0 偏移即体中心层）。

### 2.3 三个视图的固定映射

| 视图 | 显示平面 | 视线 d | 屏幕上 | 屏幕右 | right = d×up |
| --- | --- | --- | --- | --- | --- |
| 轴位 | ⊥w | +w（脚→头） | −v = A | +u = L | ✓ |
| 矢状 | ⊥u | −u（患者左→右） | +w = H | +v = P | ✓ |
| 冠状 | ⊥v | +v（前→后） | +w = H | +u = L | ✓ |

屏幕四边的标记由 `up`、`screenRight` 的实际世界向量取主导解剖轴得到（斜切时可能出现"斜方向"，此时标签为近似值；必要时显示斜切角）。

---

## 3. 旋转规则

1. **旋转轴 = 当前视图的视线方向 `d`**（在 2D 视图里只能绕它转）。
2. **顺时针为正**：屏幕顺时针拖动 θ ⇔ 把 cursor frame 绕 `d` 按右手定则转 **+θ**。
   （依据：屏幕右 = d×up，故屏幕顺时针 θ 恰等于绕 d 的 +θ。）
3. **抓取判定**：拖"十字线"（距线段 ≤6px、且不在洞内）= 旋转；拖"交点洞"（现有 10px 判定）= 移动中心。
4. **角度无限制**：不折叠到 [0°,180°)，允许转圈；三平面始终保持两两正交。
5. **谁动谁不动**：

| | 平面 | 相机 | 图像 | 十字线 | 中心点 C |
| --- | --- | --- | --- | --- | --- |
| 被转视图 | 不变 | **完全冻结** | 不变 | **在屏幕内转 θ** | 屏幕位置不变 |
| 另外两个视图 | 绕 C 转 θ | 机架**绕 C** 同角度旋转 | 显示斜切后的采样面 | **屏幕位置与姿态都不动**（仍是屏幕对齐的"＋"） | 屏幕位置**严格不变** |

> 中心点 C 是世界坐标里"三平面交点"；拖动移动中心 = 在**被拖视图所在平面内**移动 C（改另两个偏移），因此被拖视图自身层号不变、另两个视图换层——与现有行为一致。

### 3.1 相机与"中心点不动"的保证

- 每个视图的焦点不取体中心，而取
  `focal = C − S·(x₀·right + y₀·up)`，其中 `(x₀,y₀)` 是旋转开始时 C 的**归一化屏幕偏移**（常量）、`S` = 平行投影缩放；
- 由此可证 C 的两个屏幕坐标恒等于旋转前的值（与 C 是否在屏幕中心无关）；
- 代价：焦点不再等于体中心（它绕 C 公转，半径 = C 到体中心的距离），因此视野会随旋转轻微平移——这是"把 C 钉在屏幕上"的必然结果；
- 位置 = 焦点 − d·(2×体对角)；配置相机后必须调用 `ResetCameraClippingRange()`。

---

## 4. 90° 旋转预演（已审定）

下列"视线"= 相机看进去的方向；"↑ / →"= 屏幕上方/右侧对应的解剖标记。

### A. 在**轴位**视口内顺时针转 90°

| 视口 | 显示平面 | 视线 | ↑ | → | 外观 |
| --- | --- | --- | --- | --- | --- |
| 轴位（被转） | 轴位面（不变） | 脚→头（不变） | A | L | 图像完全不变；十字线屏内顺时针 90°：蓝线由竖变横、绿线由横变竖 |
| 矢状 | **冠状面**（⊥Y） | 后→前 | H | R | 标准冠状位的**水平镜像**（从背后看） |
| 冠状 | **矢状面**（⊥X） | 左→右 | H | P | 与标准矢状位**完全一致** |

### B. 在**矢状**视口内顺时针转 90°

| 视口 | 显示平面 | 视线 | ↑ | → | 外观 |
| --- | --- | --- | --- | --- | --- |
| 矢状（被转） | 矢状面（不变） | 左→右（不变） | H | P | 图像不变；十字线屏内顺时针 90° |
| **冠状** | **轴位面**（⊥Z） | **头→脚** | **P** | **L** | A 在屏幕下侧、L 在屏幕右侧 |
| 轴位 | **冠状面**（⊥Y） | 前→后 | H | L | 标准冠状位 |

### C. 在**冠状**视口内顺时针转 90°

| 视口 | 显示平面 | 视线 | ↑ | → | 外观 |
| --- | --- | --- | --- | --- | --- |
| 冠状（被转） | 冠状面（不变） | 前→后（不变） | H | L | 图像不变；十字线屏内顺时针 90° |
| 轴位 | **矢状面**（⊥X） | 右→左 | A | F | 从患者右侧看矢状面（左右镜像）+ 屏内滚 90° |
| 矢状 | **轴位面**（⊥Z） | 脚→头 | L | P | 轴位面滚 90°（L 在上、P 在右） |

### 4.1 一般规律

1. **被转视图**：平面/相机/图像/标注全不变，只有十字线在屏幕内转 θ。
2. **另两个视图**：转 90° 时**互相交换平面**（各自显示"第三个平面"）；是否镜像取决于该视口"从哪一侧看"（视口视线方向是固定约定）。
3. **十字线与中心**：另两视图里始终是屏幕对齐的"＋"，且 C 的屏幕位置严格不变 ⇒ 视觉上"不动"。
4. **0° 与 180° 的辨识**：被转视图里 180° 与 0° 完全重合（看不出来），唯一线索是**颜色方向**（蓝/绿是否互换）；而另两视图此时是镜像的。90° 时加号形状也相同，但颜色方向一定互换。

---

## 5. up 规则：连续性 + 一次性"回正"

- **默认（连续性）**：`up` 由 cursor frame 携带（跟平面一起转）⇒ 十字线永远是"＋"、位置不动、拖动手感连续；代价是"上方向"随平面滚转（如 90° 时出现脚朝上、L 朝上）。
- **为什么不能"永远头朝上"**：平面立起来后，头方向可能垂直指向相机（例：矢状位转 90° 后冠状视口显示轴位面，此时 Z 就是该平面的法向，H 根本不在图像里），"头朝上"无解；强行用投影法会在投影趋近 0 时突然翻 180°，破坏实时拖动。
- **回正（一次性）**：按 `A`/按钮，为每个视图在候选集合（8 种：视线 ±、up 取两轴之一 ±）中挑选最贴近解剖习惯的（轴位→A 上、L 右；矢/冠状→H 上、P 或 L 右）；候选全部取自 frame 轴，故十字线仍是"＋"。

---

## 6. 显示与采样

- 三个视图各一套 **`vtkImageResliceMapper` + `vtkPlane`**（`vtkImageSlice` 与窗宽窗位属性不变）；每帧只更新平面（origin = C、normal = ±n），**体数据不重采样** ⇒ 实时。
- 关键设置：`SetSlicePlane(plane)`、`SetSliceFacesCamera(0)`、`SetSliceAtFocalPoint(0)`、`SetJumpToNearestSlice(0)`（否则平面会被吸附到最近体素层）。
- **HU 读数**：直接在**原始体数据**上用世界坐标三线性采样（`geometry.toIndex(world)`），与斜切无关、精度不受显示重采样影响。
- 取景：`parallelScale` 按"当前平面 ∩ 包围盒"的外接矩形（支撑函数）适配，0° 时等于改造前的紧致取景。
  **旋转拖动期间冻结取景，且松手后继续保持冻结**（`beginRotation`/`endRotation`）⇒ 画面尺寸在整个使用过程中**不会因旋转而改变**；代价是斜切面四角可能被裁。
  需要"装得下"时用显式操作：`R` 重置、`A` 回正、改变窗口尺寸（`refitVolume`）。
  （若每帧取景，斜切到 45° 时所需宽度会达到 0° 的 `(h_u+h_v)/√2 / h_v ≈ 1.41` 倍，画面会"先缩小再扩大"；冻结后即无此"呼吸"。）
- 方向标记：按屏幕 up/right 的实际世界向量取主导轴；斜切时标签为近似值。
- 十字线：线段端点按**视口矩形**裁剪（`ScreenFrame`：焦点 + 屏幕右/上 + 视口半宽高），因此**恒铺满整个视图展示区**、且只要线经过画面就不会消失（此前按体数据包围盒裁剪，斜切/偏心时会整条消失）。
- 视图背景：三视图统一**纯黑**（`SetBackground(0,0,0)`）。
- 十字线不受遮挡：绘制时沿"朝相机方向"平移 `0.001 × 视口半高`（≈0.5 像素），使其始终压在切片图像之上（几何与命中判定仍用未偏移的线段）。
- 十字线不被裁掉：`applyFrame` 在更新十字线后刷新各视图裁剪范围（线会延伸到视口边缘，比体数据更长）。

---

## 7. 交互一览

| 操作 | 行为 |
| --- | --- |
| 拖**十字线**（≤6px，且不在洞内） | 绕当前视图视线旋转（顺时针为正，实时） |
| 拖**交点洞**（10px） | 在被拖视图平面内移动中心 C（另两视图换层） |
| 滚轮 | 当前平面沿自身法向按体素间距步进 |
| 右键拖动 | 窗宽窗位（不变） |
| `R` | 重置（frame 归零：C = 体中心、基 = 解剖轴）+ 重取景 |
| `A` / 工具条按钮 | 回正（见 §5） |
| 状态栏 | `序列 | 偏移 矢a/冠b/轴c mm | 斜切 矢x°/冠y°/轴z° | HU=… | 提示` |
| 斜切时（frame 非轴对齐） | 测量工具禁用并提示（D2） |

---

## 8. 实施方案（步骤与交付）

| 步 | 内容 | 交付/验证 |
| --- | --- | --- |
| S0 | 预检 `vtkImageResliceMapper` + `vtkPlane` | ✅ **已完成**：`m2/M0ResliceSmoke` 8 用例全 PASS（结论见 §8.1） |
| S1 | `geometry/MprCursorFrame` + `geometry/MprViewRig` | ✅ **已完成**：`MprCursorFrameTest`（11 例：正交右手、C 不动、§4 三表、平面交换、偏移/移中心语义、斜切角）＋ `MprViewRigTest`（6 例：固定映射表、up⊥d 右手、连续性携带、回正不劣于连续性、非法组合） |
| S2 | `CrosshairGeometry` 改写为 frame 版（先快照轴对齐黄金值） | ✅ **已完成**：`CrosshairGeometryTest` 7 例（3 例黄金值回归逐值一致 + 斜切断言）；`VolumeBox` + `VolumeBoxTest` 6 例 |
| S3 | 平面∩包围盒外接矩形适配（纯函数） | ✅ 并入 `VolumeBox.supportHalfExtents`（支撑函数，轴对齐时与旧取景逐值一致）+ `VolumeBoxTest` |
| S4 | `MprCameraController` 改 rig + C 钉住 + 新取景 | ✅ **已完成**：`M3RigCheck` 累积 5 次旋转（含 90°/−120°）全部 PASS——C 屏幕位移恒 0、被转视图相机冻结、三相机正对平面 |
| S5 | 显示切换（新增 `mpr/MprSlicePlaneActors`）+ `MprScene` 的 `applyFrame/moveCenter/stepActivePlane/resetView` | ✅ **已完成**：`M3MprCheck` 轴对齐回归**逐值一致**（`axial dop`、`patientLeftX/centerX/anteriorY/centerY`、`HU center/leftLung/air`） |
| S6 | `VtkMprView` 交互（拖线旋转/拖洞移动/滚轮/`A` 回正/状态栏） | ✅ 代码完成，**待 GUI 手测**（§10.3 清单）；离屏部分由 S8 覆盖 |
| S7 | `MprVolumeProbe` 改世界坐标三线性；方向标记；斜切禁用测量 | ✅ **已完成**：`probe(view, world)` + `sampleTrilinear`；标记改由各相机实际 up/right 推导；`MeasurementToolbar.setOblique` + 视图侧拦截 |
| S8 | 离屏验收 `m2/M3ResliceCheck` | ✅ **已完成**：三个 90° 用例共 30 条断言全 PASS（含 §4-B"冠状视口头→脚、A 在下、L 在右"），导出 3 张 PNG |
| S8b | 冻结性实验 `m2/M3FreezeCheck` | ✅ **已完成**：模拟连续拖动（3°×30、−2.5°×36、4°×22）后，被转视图的**渲染像素逐点一致**（最大灰度差 0；自比对照亦为 0）；被转视图的平面/相机/裁剪面**逐位相同** |
| S9 | 文档同步（本文 + `MPR业务流程.md` + `MPR功能清单.md`）+ 全量回归 | 进行中 |

### 8.1 S0 结论（已完成，8/8 PASS）

`m2/M0ResliceSmoke` 用例与结果：

| 用例 | 期望 | 实测 | 结论 |
| --- | --- | --- | --- |
| 常量 100HU（隔离窗宽窗位映射） | 灰度 166 | 166 | 映射与本项目公式**完全一致** |
| 常量 0HU | 灰度 102 | 102 | 同上 |
| 沿层交替 0/100HU、平面置于两层之间 | 50HU→灰度 134 | 134(50.2HU) | **确认为线性插值**（近邻会得 0 或 100HU） |
| 轴对齐平面（过 C） | 2×2 块最大偏差 0 | **0** | 显示 = 体数据三线性采样，**逐像素精确** |
| 斜切 45° 平面（过 C） | 0 | **0** | 斜切重采样同样精确 |
| 平面沿法向偏移 20mm（非整层） | 0 | **0** | 显式平面位置精确生效 |
| 平面沿法向偏移 26 层（整层） | 0 | **0** | 未被吸附到体素层 |
| 斜平面 + 侧视 | 0 | **0** | 平面与相机非正交时仍精确 |

**必须的配置（实现时照抄）**：

1. `VtkNativeLoader` 需加载 **`vtkRenderingImage`** 模块，否则 `vtkImageResliceMapper` 抛 `UnsatisfiedLinkError`（本项目已加入加载清单）。
2. 相机配置后必须 `renderer.ResetCameraClippingRange()`，否则远裁剪面把整幅切掉（全黑）。
3. mapper 必须显式设置：`SetSlicePlane(plane)`、`SetSliceFacesCamera(0)`、`SetSliceAtFocalPoint(0)`、`SetJumpToNearestSlice(0)`、`SetSeparateWindowLevelOperation(1)`、`SetAutoAdjustImageQuality(0)`、`SetImageSampleFactor(2)`、`SetResampleToScreenPixels(1)`，并 `SetInterpolator(vtkImageInterpolator with 线性模式)`。
4. **校验方法学（重要）**：不要用"单个中心像素"做判据——屏幕中心与像素中心存在半像素几何歧义，乘以局部 HU 梯度会放大成 10~20HU 的**假误差**。正确做法：平行投影下由相机参数解析反算像素中心的世界点
   `p = focal + right·dx + up·dy`，再沿视线与实际显示平面求交（偏移平面不在焦点平面上），本检查按此法得 **2×2 块最大灰度偏差 = 0**。
5. 附带发现：`vtkImageResliceMapper` 自带 **slab/MIP API**（`SetSlabThickness`、`SetSlabTypeToMean|Min|Max|Sum`、`SetSlabSampleFactor`）→ **C5 厚层与 C6 MIP 可直接复用**，且能与 C3 的斜切平面叠加。

### 8.2 实施中修掉的问题（存档）

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 未载入序列时鼠标划过画布抛 NPE | `MprScene.getFrame()` 仅载入后才有值，`updateStatus()` 直接取用 | `updateStatus`/测量分支加 `isVolumeReady()` 判空；`getFrame()` 注明可能为 null |
| "视图转着转着自己也变了" | 拖动中指针离开该视图视口后，`activeView()` 返回**另一个**视图 → 旋转轴被切走，原视图平面随之改变 | 在 `onLeftButtonDown` **锁定起始视图**（`rotationView`），整个拖动过程不再改变旋转轴 |
| "另两个视图先缩小再扩大" | 每帧按朝向取景：斜切到 45° 时平面所需宽度是 0° 的 `(h_u+h_v)/√2 / h_v` ≈ **1.41 倍**（本数据 194→274mm），90° 又回到 1.0 倍 | 按 D1：`beginRotation()` 冻结三视图 `parallelScale` 且**松手后继续冻结**（允许裁角），仅 `R`/`A`/窗口尺寸变化才重取景；`M3FreezeCheck` 断言"拖动中冻结 + 松手后仍冻结" |
| "先转轴位 90°，再碰冠状位时轴位视图瞬间歪 90°" | 轴位拖动时其相机被冻结（up 保持 A），而光标坐标系的 u/v 已绕 w 转 90°；下次配置该视图时若直接用**机架轴** `−v′`，up 就变成 L（患者左朝上）⇒ 图像歪 90° | `chooseUp()` 改为**滚动连续性**：把该视图*上一次的 up* 连续投影到新平面（`MprViewRig.projectOntoPlane`，退化才退回机架轴）；`M3RigCheck` 新增"链式旋转"断言：轴位 up 变化 0.5°（修复前 60~90°）|
| 旋转时**十字线可能整条消失** | 线段原本按**体数据包围盒**裁剪：斜切/偏心后痕迹线与包围盒可能没有交点 ⇒ 直接判为不可见 | 改为按**视口矩形**裁剪（新增纯数学 `geometry/ScreenFrame` + `CrosshairGeometry.clipToRect`）：只要线通过画面内就一定有交点，结构上不会消失 |
| 十字线希望**铺满整个视图展示区** | 同上（原来最多只到体数据边界） | 线段的两个外侧端点落在视口边界上（`CrosshairGeometryTest.shouldSpanWholeViewportForAxisAlignedFrame` 逐点断言 = 交点 ± 半宽/半高）|
| 视图背景希望**纯黑** | 原来三个视图用深灰/深绿/深蓝区分 | `MprScene.configureViewports` 统一 `SetBackground(0,0,0)` |
| 旋转时十字线**被切片图像遮挡** | 十字线与切片图像**完全共面** ⇒ 深度测试平手，图像（先加入 renderer）压掉了线 | 绘制时把线沿"朝相机方向"（−视线）平移 `0.001 × 半高`（≈0.5 像素，`MprCrosshairOverlay.apply`）；几何/命中判定仍用未偏移的数学线段，互不影响 |
| 旋转时十字线**整段不显示** | 线现在延伸到**视口边缘**（比体数据更长），而相机近/远裁剪面是按"更新十字线之前"的演员包围盒算的 ⇒ 远端被裁掉 | `MprScene.applyFrame` 在 `crosshairs.update` 之后对三个 renderer 各调一次 `ResetCameraClippingRange()` |
| 疑似"被转视图变了"（离屏校验报 FAIL） | 校验脚本的视口像素区域坐标写错（矢状视口 y 区间为空），并非软件缺陷 | 修正区域映射；新增 `M3FreezeCheck`：模拟连续拖动后比对被转视口像素，**逐点差 0**，并带"同状态连导两次"自比对照 |

---

## 9. 验收断言（离屏 `M3ResliceCheck`）

程序化在三个视图各旋转 90° 后断言：

1. 另两个视图**显示的平面法向**符合 §4 三表；
2. 另两个视图 `worldToDisplay(C)` **与旋转前一致**（= "中心点不移动"的机器验证）；
3. 另两个视图的**十字线方向平行于屏幕轴**（仍是"＋"）；
4. **被转视图**的相机与平面**完全不变**，且其十字线恰好在屏内转了 90°；
5. 导出 PNG 供目视（三视图各一张）。

---

## 10. 验证手册（每步怎么自己验）

### 10.1 三条命令

```powershell
# 1) 一键验证当前状态：规范扫描 + 全部单测 + -Pvtk 编译 + 全部离屏检查（缺失的检查会自动跳过）
powershell -ExecutionPolicy Bypass -File scripts\verify.ps1 -DataDir J:\workspace\DicomMPR\3120221229008001

# 2) 只跑某个离屏检查（会先 -Pvtk 编译）
powershell -ExecutionPolicy Bypass -File scripts\run-check.ps1 -MainClass com.zlyd.mpr.m2.M0ResliceSmoke -Args J:\workspace\DicomMPR\3120221229008001

# 3) 启动 GUI 手测
powershell -ExecutionPolicy Bypass -File scripts\run-app.ps1
```

只想跑某几个单测：`mvn -B test -Dtest=MprCursorFrameTest,MprViewRigTest`。
只看规范：`scripts\verify.ps1 -StandardsOnly`。

### 10.2 逐步验证对照

| 步 | 命令 | 看什么 | 通过判据 |
| --- | --- | --- | --- |
| S0 ✅ | `run-check.ps1 -MainClass ...M0ResliceSmoke -Args <数据>` | 8 行 `-> PASS` 与末行 `S0 结论: true`；`target\reslice\*.png` 可打开 | 全 PASS |
| S1 ✅ | `mvn -B test -Dtest=MprCursorFrameTest,MprViewRigTest` | 11 + 6 个用例（用例名即 §4 表的断言） | 全绿 |
| S2 | `mvn -B test -Dtest=CrosshairGeometryTest,VolumeBoxTest` | ①轴对齐"黄金值"回归（与改造前逐值一致）②斜切断言（线向=两平面交线、⊥法向、留洞）③盒裁剪/支撑半长 | 全绿（黄金值不一致即视为回归失败） |
| S3 | （并入上一条）`mvn -B test -Dtest=VolumeBoxTest` | 0° = 面矩形、45° = (h₁+h₂)/√2、退化情形 | 全绿 |
| S4 | `run-check.ps1 -MainClass ...M3RigCheck -Args <数据>` | 每次旋转打印"另两视图交点屏幕位移""被转视图相机冻结""三相机正对平面"；末行"链式旋转"轴位 up 变化 | 全 PASS，位移 = 0.000000，链式 up 变化 ≤ 2° |
| S5 | `verify.ps1 -DataDir <数据>` | `M3MprCheck` 的 `axial dop/patientLeftX/anteriorY/HU` 与改造前**逐值一致**；`M3RigCheck` 仍绿 | 一致 + 全绿 |
| S6 | `run-app.ps1` 手测 | 见 §10.3 手测清单 | 逐条符合 |
| S7 | `run-check.ps1 -MainClass ...M3ResliceCheck` + GUI | 斜切前后把鼠标放在十字线交点上：HU 读数不变；斜切时测量工具禁用并提示 | PASS + 人工确认 |
| S8 | `run-check.ps1 -MainClass ...M3ResliceCheck -Args <数据>` | 三个 90° 用例逐条 `[PASS]`（§4 三表、交点位移 0.000000、十字线平行屏幕轴、被转视图冻结、十字线转 90.00°）+ `target\reslice\s8_view{0,1,2}_90.png` | 全 PASS，PNG 方向可目视核对 |
| S8b | `run-check.ps1 -MainClass ...M3FreezeCheck -Args <数据>` | 每例的"自比对照"/"被转视口"最大灰度差 + "拖动中三视图取景冻结" | 灰度差均为 0；取景 scale 前后完全一致（`[PASS]`） |
| S8c | `run-check.ps1 -MainClass ...M3CrosshairCheck -Args <数据>` | 4 个状态（0°/轴位 90°/再转冠状 −60°/再转矢状 120°）× 各视口 2 种配色的**像素数** | 每条线 ≥ 60 像素（实测 330~780），全 PASS |
| S9 | `verify.ps1 -DataDir <数据>` | 四段输出与末行 `VERIFY OK` | OK |

> `verify.ps1` 对**尚未实现**的检查会打印 `-- skip ... (not built yet)`，所以任何阶段都能安全地跑一键验证。

### 10.3 S6 GUI 手测清单（对照 §4/§7）

1. 右键序列 →「MPR」，出现三视图 + 两行工具条；
2. **拖轴位竖线（蓝）**：轴位图像/取景完全不变、蓝线在屏内转动；另两视图十字线保持"＋"且位置不动，只有图像变成斜切面；
3. 在轴位转到 **90°**：矢状视口 = 冠状面（R 在右侧）、冠状视口 = 标准矢状位（§4-A）；轴位加号与 0° 形状相同，但蓝/绿线的横竖方向互换（凭颜色判断）；
4. **拖矢状线到 90°**：冠状视口 = 轴位面、视线"头→脚"、A 在下方、L 在右侧（§4-B）；
5. **中心点不动**：先拖洞把交点移到偏离屏幕中心处（如左上），再在其他视图旋转 → 该点在屏幕上**原地不动**；
6. **拖洞移动中心**：被拖视图层号不变、另两视图换层、交点跟手；
7. **滚轮**：沿该平面法向翻层（斜切后沿斜法向）；
8. `R` 重置（回解剖轴 + 重取景）；`A` 回正（图像滚正，十字线仍是"＋"）；
9. **颜色**：蓝 = 矢状面 X=i、绿 = 冠状面 Y=j、红 = 轴位面 Z=k；
10. **状态栏**：`偏移 矢a/冠b/轴c mm`、`斜切 矢x°/冠y°/轴z°`、`HU=…`、提示；斜切后角度应显示 90° 等；
11. 斜切状态下测量工具禁用并有提示。

### 10.4 小技巧：变异检验（确认单测真的能咬住）

关键数学用例最好做一次"故意改坏"的确认：例如把 `MprViewRig.continuity()` 里轴位的 up 符号由 `-1` 改成 `1`，或把 `MprCursorFrame.rotate` 的旋转方向取反，重跑对应测试应当**立刻失败**；改回后应恢复全绿。这样能确认测试不是在"空转"。

---

## 11. 术语

| 术语 | 含义 |
| --- | --- |
| `verify.ps1` | 一键验证：规范扫描 + 单测 + `-Pvtk` 编译 + 全部离屏检查 |
| `run-check.ps1` | 运行单个离屏检查 main（自动 `-Pvtk` 编译） |
| cursor frame | 光标坐标系 `(u,v,w)`，决定三个平面的法向 |
| 平面偏移 a/b/c | 三个平面沿 u/v/w 相对体中心的偏移（mm），对应旧实现的 i/j/k |
| 中心点 C | 三平面唯一交点，十字线交点即 C |
| rig（机架） | 某视图的相机三元组（位置/焦点/up）；旋转时"机架绕 C"旋转 |
| 连续性 up | up 由 cursor frame 携带（默认规则） |
| 回正 | 一次性把各视图 roll 摆到最贴近解剖习惯的方向 |
