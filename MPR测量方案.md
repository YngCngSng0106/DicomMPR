# MPR 测量功能扩展方案（待确认）

> 目标：在现有 F1/F2 测量（长度/角度/矩形ROI/椭圆ROI）基础上，扩展为
> **线段、矩形、椭圆、角度、曲线、自由形状** 六种测量，并把 **删除选中 / 全部删除**
> 放进同一个测量下拉；同时把"十字线/平移"移出下拉、把"重置"移到第二排。

---

## 1. 交互设计（各工具的落点、结束方式、输出）

| 工具 | 落点 / 操作 | 结束方式 | 输出 |
| --- | --- | --- | --- |
| 线段 | 2 点 | 自动完成 | 长度 `mm` |
| 矩形 | 2 点（对角） | 自动完成 | 面积 `cm²` + HU 均值(min, max, n) |
| 椭圆 | 2 点（外接矩形对角） | 自动完成 | 面积 `cm²` + HU 统计 |
| 角度 | 3 点（第 2 点为顶点） | 自动完成 | 角度 `°` |
| 曲线 | 多点折线 | **双击 / 右键 / Enter** | 总长度 `mm`（可附段数） |
| 自由形状 | 按住左键拖动描画轮廓 | 松开左键即闭合 | 面积 `cm²` + 周长 `mm` + HU 统计 |

- 绘制中始终有"跟随指针的预览"（沿用现有 `updatePreview`）。
- 右键单击 = 取消未完成测量（沿用现有行为）；对"曲线"额外作为"结束并完成"。

## 2. 工具条布局（最终）

| 行 | 内容 |
| --- | --- |
| 第 1 行 | `导出 PNG` · `等体素重采样` · 窗宽窗位预设 · `清理缓存` |
| 第 2 行 | `测量:` **[下拉]** · `十字线/平移`（独立按钮） · `重置` · 结果标签 · 提示标签 |

下拉项（一个列表，混合"工具"与"动作"）：

```
线段 / 矩形 / 椭圆 / 角度 / 曲线 / 自由形状
──────────────
删除选中
全部删除
```

- **不再包含"十字线/平移"**：改为第 2 行上的独立按钮（按下态表示当前是十字线模式）；另外 `Esc` 也可随时退出测量回到十字线模式。
- **重置**：第 2 行按钮，等价于现在的 `R`（重置视图/光标）；`A` 回正是否也放过去见 Q5。
- `删除选中 / 全部删除` 是**动作项**：选中即执行，随后下拉自动回到原工具（避免误以为切换了工具）。

## 3. 选中与删除

- **选中**：在 `十字线/平移` 模式下，左键**点中**某条测量（距其线段/轮廓 ≤6px）即选中并高亮（加粗 + 高亮色），结果标签显示其数值；点空白处取消选中。
  - 优先级：**命中测量 > 跳转中心**（即点中测量时不再移动十字线中心）。
- **删除选中**：删除当前选中的测量；无选中时状态栏提示"未选中测量"，不执行删除。另支持 `Delete` 键。
- **全部删除**：删除所有测量（即现有"清除测量"按钮，改名为下拉项）。
- 每完成一条测量**自动选中它**，便于立刻核对/删除。

## 4. 纯数学层（全部加单元测试）

| 类 | 改动 |
| --- | --- |
| `geometry/MeasurementType` | 新增 `CURVE("曲线")`、`FREEHAND("自由形状")`；`isRoi()` 覆盖 `FREEHAND`；新增"是否拖动式/是否多点"判定 |
| `geometry/MeasurementCalculator` | 新增 `polylineLength(points)`（折线总长）、`polygonArea(points, axis1, axis2)`（鞋带公式）、`polygonPerimeter(points, axis1, axis2)`、`polygonContains(i, j, points, axis1, axis2)`（射线法，用于自由形状 HU 取样） |
| `geometry/Measurement` | 结构不变（`points` 本就是列表，可容纳多点） |
| `util/MeasurementFormatter` | 增加"曲线（长度）""自由形状（面积 + 周长 + HU）"的文本格式 |

## 5. 渲染与交互层改动

| 文件 | 改动 |
| --- | --- |
| `mpr/MprMeasurementOverlay` | 绘制折线（多段）与自由形状多边形；**选中高亮**；新增"点到测量的距离"命中判定 |
| `mpr/MprMeasurementController` | 曲线（多点累积 + 结束条件）、自由形状（拖动采样 → 闭合）；选中/取消选中；`deleteSelected()`；完成即选中 |
| `mpr/MprScene` | 暴露 `selectMeasurementAt(displayX, displayY)` / `deleteSelectedMeasurement()` / `getSelectedMeasurement()`；清理缓存时一并清空选中态 |
| `ui/MeasurementToolbar` | 下拉重构（6 工具 + 2 动作）；移出"十字线/平移"→ 独立按钮；新增 `重置` 按钮；结果/提示标签 |
| `mpr/VtkMprView` | 十字线模式：先判命中测量→选中，否则跳转中心；曲线双击结束；自由形状拖动（按下开始、移动采样、松开闭合）；`Delete`/`Esc` 键；`重置` 按钮接线 |
| 校验 | `M3MeasurementCheck`（新）：离屏构造各类型测量，断言数值/面积/HU 统计、选中与删除生效 |

## 6. 已确认的决定（Q1–Q7）

| 问题 | 决定 |
| --- | --- |
| Q1 曲线 | 按**折线**实现（多点直线段，输出总长度）；结束方式 = **双击 + Enter** |
| Q2 自由形状 | **不做平滑**，保留原始描画点；输出 面积 + 周长 + HU 统计 |
| Q3 选中方式 | 按方案：十字线模式下点中测量即选中，命中测量优先于跳转中心 |
| Q4 删除选中 | 无选中时**提示**不删除；完成一条测量后**自动选中**它 |
| Q5 第二排按钮 | **重置 + 回正都放第二排** |
| Q6 曲线 HU | **不做** HU 统计（仅长度） |
| Q7 斜切与测量 | **斜切下允许测量**（撤销原 D2 禁用）；**转动某视图时：该视图的测量保持不动，其他视图的测量全部清空** |

### 6.1 由 Q7 引出的关键改造：测量的平面基准

现在测量的面积/包含判定/ROI 取样都是按**体数据解剖轴**（`MprViewOrientation.screenRight/up`）算的，
斜切后会算错。因此统一改为**按当前光标坐标系（`MprCursorFrame`）该视图的平面**取基准：

- 平面坐标 = 世界点在该平面两个轴向（frame 的两个面内轴）上的投影；
- 面积/周长/包含判定/ROI 取样全部在这个平面坐标下进行（`geometry.toIndex` 负责任意世界点 → 体素索引）；
- 视图旋转时，其平面法向不变（旋转是绕它自己的法向），故该视图已有测量**仍然有效、位置不动**；
  其他视图平面法向已变，其测量**全部清空**（避免悬空/算错）。

因此 `MprMeasurementOverlay` / `MprMeasurementController` 的平面参数由 `VolumeGeometry` 改为
`MprCursorFrame`（每帧随 `applyFrame` 刷新），`MprScene` 负责在旋转后清空其他视图的测量。

## 7. 实施顺序（每步可独立验证）

| 步 | 内容 | 验证 | 状态 |
| --- | --- | --- | --- |
| S1 | 纯数学：类型 + 计算器（折线/多边形/包含）+ 单测 | `mvn test` | ✅ 完成（69 单测，含斜平面多边形用例） |
| S2 | 绘制层：折线、自由形状、选中高亮 + 命中判定 | 单测 + 离屏 | ✅ 完成（平面基准改为按视图相机 `ScreenFrame`，抽出 `VtkScreenFrames` 共用） |
| S3 | 控制器：多点（曲线）与拖动（自由形状）流程、选中/删除 | 单测 + 离屏 | ✅ 完成（ROI 取样改为平面 mm 采样 + 三线性取值，支持斜切） |
| S4 | 工具条与交互：下拉重构、移出十字线、重置入第二排、`Delete`/`Esc` | GUI 手测 | ✅ 代码完成，**待 GUI 手测** |
| S5 | 新增 `M3MeasurementCheck` 离屏校验；文档同步；全量回归 | `scripts/verify.ps1` | ✅ 完成（10 条断言全 PASS；一键校验现含 8 个离屏检查） |

### 实施后的行为差异（相对旧版）

- 旧版：下拉首项是"十字线/平移"、只有 4 种测量、无选中/删除选中、斜切时禁用测量。
- 新版：下拉只有 6 种测量 + 2 个删除动作；十字线改为独立按钮；新增 `重置`/`回正` 按钮；支持选中高亮与删除选中；
  **斜切下可测量**，且旋转时"本视图测量保留、其他视图测量清空"。
